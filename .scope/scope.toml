schema = "scope/project/v1"
id = "scope.project"
kind = "project"

[paths]
knowledge = "knowledge.toml"
policy_environment = "policy/environment.toml"
policy_records = ["policy/records.toml"]
checklists = "checklists"
tests = "tests"
state = "state"
