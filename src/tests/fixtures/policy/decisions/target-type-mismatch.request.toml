schema = "terminal-policy/request/v1"
request_id = "request.decision.target-type-mismatch"
principal = "principal.example"
operation = "filesystem.write"
targets = ["path:src/main.rs", "repository:scope"]

[session]
id = "session.decision"

[[context]]
provider = "branch"
key = "name"
value = "feature/example"
provenance = "fixture.branch/v1"
digest = "sha256:fixture-target-type-mismatch"
