# Publication

This directory owns the source definitions for KERO's generated `canary`,
`beta`, and `stable` repositories. Disposable previews and staged payloads live
outside the source repository under `.heap/source/`.
